import React from 'react';
import {
  Dimensions,
  StyleSheet,
  ScrollView,
  View,
  Image,
  Text,
} from 'react-native';

const styles = StyleSheet.create({});

export default class PreferenceView extends React.Component {
  render() {
    return (
      <View style={{flex: 1}}>
        <Text>PreferenceView</Text>
      </View>
    );
  }
}
