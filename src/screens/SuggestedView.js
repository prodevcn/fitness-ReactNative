import React from 'react';
import {
    Dimensions,
    StyleSheet,
    ScrollView,
    View,
    Image,
    Text,
} from 'react-native';

const styles = StyleSheet.create({

});

export default class SuggestedView extends React.Component {
    render() {
        return (
            <View style={{ flex: 1 }}>
                <Text>Suggested</Text>
            </View>
        );
    }
}
